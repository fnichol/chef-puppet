# Generated by Chef (lol) for <%= node['fqdn'] %>
# Local modifications will be overridden

node default {
}

# Matches Vagrant virtual machines
node /^.*\.vagrantup\.com/ {
<% @modules.each do |mod| -%>
include "<%= mod %>"
<% end -%>
}

# Matches Docker Linux hostnames
node /^[0-9a-z]{12}/ {
<% @modules.each do |mod| -%>
include "<%= mod %>"
<% end -%>
}
