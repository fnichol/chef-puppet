---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.5

suites:
  - name: default
    run_list:
      - recipe[puppet::client]
    attributes:
      puppet:
        client_conf:
          main:
            server: 'localhost'
